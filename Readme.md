


I built a full-stack prototype where the Vue frontend uploads documents to a Node backend. The backend triggers an asynchronous 'AI agent' simulation that processes data in the background while the user continues working, and the UI updates automatically once the processing is done.